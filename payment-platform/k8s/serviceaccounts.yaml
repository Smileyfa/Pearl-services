apiVersion: v1
kind: ServiceAccount
metadata:
  name: pearlpay-api-sa
  namespace: default
# Disable token automounting so pods do not receive Kubernetes API credentials unless explicitly needed.
automountServiceAccountToken: false
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: pearlpay-frontend-sa
  namespace: default
# Disable token automounting so pods do not receive Kubernetes API credentials unless explicitly needed.
automountServiceAccountToken: false
